// ============================================================
// 前端运行时配置 — 部署后改本文件即可，无需重新构建
// Vite 会把 public/ 原样复制到 dist/ 根目录，index.html 在应用加载前读取
// ============================================================
window.__APP_CONFIG__ = {
  // API 基础路径：同域 Nginx 反代用相对 /api；跨域部署填完整地址，如 https://api.example.com
  apiBaseUrl: '/api',
  // 应用标题（浏览器标签页 title 后缀）
  appTitle: '支付商户管理平台',
}
