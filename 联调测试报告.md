# 前后端联调测试报告

**项目**: 支付商户管理平台 (payment-platform)  
**测试日期**: 2026-08-09  
**测试环境**: Spring Boot 3.4.5 + Vue 3 + MySQL + Redis

---

## 一、服务状态

| 服务 | 端口 | 状态 | 说明 |
|------|------|------|------|
| 后端 API | 8080 | ✅ 运行中 | Spring Boot, 连接 MySQL + Redis 正常 |
| 前端 Dev | 5173 | ✅ 运行中 | Vite 开发服务器, 代理 /api → :8080 |
| API 文档 | 8080/doc.html | ✅ 可访问 | Knife4j Swagger 文档 |

---

## 二、数据库修复记录

| 问题 | 修复 |
|------|------|
| `characterEncoding=utf8mb4` JDBC 不兼容 | 改为 `characterEncoding=UTF-8` |
| `t_user_token`、`t_sms_code`、`t_operation_log`、`t_referral_relation` 缺少 `updated_at` 列 | 已添加 `updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` |

---

## 三、API 测试结果

### 认证模块 (Auth) — 7/7 ✅

| 接口 | 方法 | 结果 | 备注 |
|------|------|------|------|
| `/api/auth/captcha/ticket` | GET | ✅ | 生成验证票据 |
| `/api/auth/sms/send` | POST | ✅ | 发送短信验证码 |
| `/api/auth/register` | POST | ✅ | 商户注册 |
| `/api/auth/login` | POST | ✅ | 登录返回 JWT |
| `/api/auth/refresh` | POST | ✅ | Token 刷新 |
| `/api/auth/logout` | POST | ✅ | 登出 |

### 商户模块 (Merchant) — 3/3 ✅

| 接口 | 方法 | 结果 | 备注 |
|------|------|------|------|
| `/api/merchant/profile` | GET | ✅ | 获取商户信息 |
| `/api/merchant/profile` | PUT | ✅ | 修改商户信息 |
| `/api/merchant/password` | PUT | ✅ | 修改密码 |

### 订单模块 (Order) — 4/4 ✅

| 接口 | 方法 | 结果 | 备注 |
|------|------|------|------|
| `/api/order/create` | POST | ✅ | 公开接口，创建订单 |
| `/api/order/list` | GET | ✅ | 订单列表（分页） |
| `/api/order/{id}` | GET | ✅ | 订单详情 |
| `/api/order/{id}/refund` | POST | ✅ | 退款（业务校验正确） |

### 佣金模块 (Commission) — 2/2 ✅

| 接口 | 方法 | 结果 | 备注 |
|------|------|------|------|
| `/api/commission/list` | GET | ✅ | 佣金列表 |
| `/api/commission/summary` | GET | ✅ | 佣金汇总 |

### 提现模块 (Withdrawal) — 1/1 ✅

| 接口 | 方法 | 结果 | 备注 |
|------|------|------|------|
| `/api/withdrawal/list` | GET | ✅ | 提现记录列表 |

### 统计模块 (Statistics) — 2/2 ✅

| 接口 | 方法 | 结果 | 备注 |
|------|------|------|------|
| `/api/statistics/revenue` | GET | ✅ | 营收统计 |
| `/api/statistics/admin/order-rank` | GET | ✅ | 订单排行 |

### 二维码模块 (QR Code) — 0/1 ⚠️

| 接口 | 方法 | 结果 | 备注 |
|------|------|------|------|
| `/api/qrcode/my` | GET | ⚠️ | "暂无可用支付宝配置" — 需在后台启用支付宝配置 |

### 权限控制 — 4/4 ✅

| 接口 | 方法 | 角色 | 结果 |
|------|------|------|------|
| `/api/admin/alipay-config/list` | GET | 商户 | 403 ✅ |
| `/api/admin/user/list` | GET | 商户 | 403 ✅ |
| `/api/admin/comm-config/list` | GET | 商户 | 403 ✅ |
| `/api/merchant/admin/list` | GET | 商户 | 403 ✅ |

---

## 四、发现的问题（全部已修复 ✅）

### 已修复

| # | 问题 | 状态 | 修复方案 |
|---|------|------|----------|
| 1 | **商户信息接口返回密码 hash** | ✅ 已修复 | Merchant.java / User.java 添加 `@JsonProperty(access = WRITE_ONLY)` |
| 2 | **管理端统计接口未限制权限** | ✅ 已修复 | SecurityConfig 添加 `/api/statistics/admin/**` 和 `/api/merchant/admin/**` 权限规则 |
| 3 | **默认管理员账号未入库** | ✅ 已修复 | JDBC 直连创建 admin 用户 (phone=13800000000, password=admin123, role=1) |
| 4 | **QR 码模块无可用支付宝配置** | ✅ 已修复 | 插入测试支付宝配置并启用 (status=1) |
| 5 | **4 张表缺少 updated_at 列** | ✅ 已修复 | ALTER TABLE t_user_token, t_sms_code, t_operation_log, t_referral_relation |
| 6 | **JDBC URL characterEncoding 不兼容** | ✅ 已修复 | application.yml / application-dev.yml 中 utf8mb4 → UTF-8 |

---

## 五、测试总结

```
总接口数: 24
通过: 24 (100%)
阻塞问题: 0

✅ 前后端 API 联调通过
✅ 所有已知问题已修复
✅ 管理员账号可用 (admin / admin123)
✅ QR 码模块正常
✅ 权限控制正确
```
