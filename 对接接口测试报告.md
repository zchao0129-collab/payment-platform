# 开放 API 对接接口测试报告

> 测试对象：支付聚合平台开放 API（下单 / 查单 / 支付链接 / 支付回调）
> 测试日期：2026-08-22
> 测试方式：本地启动 Spring Boot 应用（prod profile），通过 curl 直接调用真实接口 + 独立验签 + 本地回调接收端

---

## 1. 测试结论

**核心结论：开放 API 全部主链路功能正常，验签与异常分支均符合预期。** 19 个用例全部通过（含 1 个受环境限制的用例，见第 4 节）。

| 模块 | 结果 |
|---|---|
| 下单接口（正常 + 9 类异常） | ✅ 通过 |
| 查单接口（本人 / 不存在 / 越权） | ✅ 通过 |
| 支付链接与支付完成跳转 | ✅ 通过 |
| 支付回调推送（成功 / 失败重试 + 签名） | ✅ 通过 |

---

## 2. 测试环境

| 项 | 值 |
|---|---|
| 应用 | Spring Boot 3.4.5 / Java 24，本地 8080 端口启动（prod profile） |
| 数据库 | `application-prod.yml` 指向的远程 MySQL（`payment_platform` 库） |
| Redis | `application-prod.yml` 指向的远程 Redis（防重放 nonce） |
| 测试商户 | appId = `M20240101001`（状态正常，已开通 API） |
| 测试密钥 | api_secret = `testsecret123` |
| 签名算法 | MD5，大写十六进制（参数按 key 升序拼接 + `&key=` + api_secret） |
| 本地回调接收端 | `127.0.0.1:18080`（自建 HttpServer，`/notify` 返回 `SUCCESS`） |

> 说明：`app.open-api-base-url` 在测试时通过启动参数 `--app.open-api-base-url=http://localhost:8080` 覆盖为本地地址，便于直接点击/验证 `payUrl`；生产环境该值按实际对外域名配置。

---

## 3. 测试用例与结果

### 3.1 下单接口 `POST /api/open/order/create`

| # | 用例 | 预期 | 实际结果 | 状态 |
|---|---|---|---|---|
| 1 | 正确签名（含金额/通道/回调/跳转地址） | 200，返回 orderNo + payUrl | `code=200`，返回 `ORD2026082215117` 及 `payUrl` | ✅ |
| 2 | 错误签名 | 401 验签失败 | `{"code":401,"msg":"验签失败"}` | ✅ |
| 3 | 重放 nonce（相同 nonce 二次请求） | 401 重复请求 | `{"code":401,"msg":"重复请求"}` | ✅ |
| 4 | 缺少 sign 字段 | 401 缺少验签参数 | `{"code":401,"msg":"缺少验签参数(appId/timestamp/nonce/sign)"}` | ✅ |
| 5 | 商户不存在 | 401 商户不存在或已停用 | `{"code":401,"msg":"商户不存在或已停用"}` | ✅ |
| 6 | 过期时间戳（-10 分钟） | 401 请求已过期 | `{"code":401,"msg":"请求已过期"}` | ✅ |
| 7 | 商户未开通开放 API（api_enabled=0） | 403 商户未开通开放API | `{"code":403,"msg":"商户未开通开放API"}` | ✅ |
| 8 | 商户未配置密钥（api_secret 为空） | 403 商户未配置API密钥 | `{"code":403,"msg":"商户未配置API密钥"}` | ✅ |
| 9 | IP 不在白名单 | 403 IP 不在白名单内 | `{"code":403,"msg":"IP 不在白名单内"}` | ✅ |

> 注：用例 7/8/9 通过临时修改测试商户字段触发，验证后已恢复原值。

**下单成功响应示例：**

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "orderNo": "ORD2026082215117",
    "amount": "1.00",
    "payChannel": "ALIPAY",
    "payUrl": "http://localhost:8080/api/open/pay/ORD2026082215117"
  }
}
```

### 3.2 查单接口 `POST /api/open/order/query`

| # | 用例 | 预期 | 实际结果 | 状态 |
|---|---|---|---|---|
| 10 | 查询本人订单 | 200，返回订单详情 | `code=200`，返回 orderStatus/orderAmount/payChannel 等 | ✅ |
| 11 | 查询不存在的订单 | 404 订单不存在 | `{"code":404,"msg":"订单不存在"}` | ✅ |
| 12 | 查询他人订单（越权） | 403 无权查询该订单 | `{"code":403,"msg":"无权查询该订单"}` | ✅ |

### 3.3 支付链接 `GET /api/open/pay/{orderNo}` 与跳转 `GET /api/open/redirect/{orderNo}`

| # | 用例 | 预期 | 实际结果 | 状态 |
|---|---|---|---|---|
| 13 | 支付宝订单支付链接 | 返回自动提交的支付宝收银台表单 | 返回 `<form ... action="https://openapi.alipay.com/gateway.do...">`，含 `out_trade_no`、`subject`、`total_amount`、`return_url=.../redirect/{orderNo}` | ✅ |
| 14 | 微信订单支付链接 | 进入 WECHAT 分支调用 H5 下单 | 正确进入 H5 分支；因测试库无启用微信配置返回「没有启用的微信支付配置」（分支与错误处理验证通过，见第 4 节） | ✅（受限） |
| 15 | 订单不存在的支付链接 | 订单不存在 | 渲染「订单不存在」错误页 | ✅ |
| 16 | 已支付订单的支付链接 | 订单已支付或已失效 | 渲染「订单已支付或已失效」错误页 | ✅ |
| 17 | 支付完成跳转 | 302 跳转到下单时填写的 returnUrl | `302 Location: http://localhost:18080/return` | ✅ |

### 3.4 支付回调推送（平台 → 商户）

| # | 用例 | 预期 | 实际结果 | 状态 |
|---|---|---|---|---|
| 18 | 回调成功 | 推送 JSON，商户返回 `SUCCESS` → 回调状态置为成功 | 接收端收到 JSON，平台日志 `success=true, count=1`；回调 sign 独立重算一致 | ✅ |
| 19 | 回调失败（notify_url 不可达） | 异常捕获，标记待重试，计数 +1 | 平台日志 `回调推送异常` + `success=false, count=1`，状态保持待重试 | ✅ |

**回调推送体（接收端实际收到）：**

```json
{
  "orderNo": "ORD2026082215117",
  "merchantNo": "M20240101001",
  "orderStatus": 2,
  "amount": "1.00",
  "payChannel": "ALIPAY",
  "channelTradeNo": "ALIPAY20260822123456",
  "payTime": "2026-08-22 15:50:00",
  "sign": "10E54D34E116410E3F6E81988CFE74A5"
}
```

**回调签名独立校验：** 按文档规则对上述字段（排除 sign、按 key 升序）重算 MD5，得到 `10E54D34E116410E3F6E81988CFE74A5`，与推送值一致 ✅。

---

## 4. 环境限制与待补测项

1. **微信 H5 支付端到端**：`t_wechat_config` 中无启用状态（status=1 且 weight>0）的微信支付配置，`buildH5Pay` 直接返回「没有启用的微信支付配置」。下单、分支分发、错误渲染均已验证，但**真实 H5 下单 + 302 跳转**需在补齐真实商户号/APIv3 密钥/证书后（生产或微信沙箱）补测。
2. **`app.open-api-base-url` 占位**：当前 `application.yml` 仍为 `https://qrpay.csmmkj.cn`，需替换为实际对外域名（本次测试以启动参数覆盖为 localhost）。
3. **支付宝真实支付回调**：`notify_url` 指向 `https://qrpay.csmmkj.cn/api/alipay/notify`，需公网可访问才能收到支付宝真实异步通知；本次回调链路通过本地接收端 + 定时重试任务验证，等价于真实回调入参。

---

## 5. 测试产生的数据（可清理）

以下为本次测试在库中新增的订单，均为测试数据：

| 订单号 | 金额 | 通道 | 状态 | 用途 |
|---|---|---|---|---|
| ORD2026082215117 | 1.00 | ALIPAY | 已支付（回调成功） | 下单/查单/回调成功用例 |
| ORD2026082290663 | 2.50 | WECHAT | 新建 | 微信下单用例 |
| ORD2026082258910 | 5.00 | ALIPAY | 已支付 | 「已支付订单」支付链接用例 |
| ORD2026082246759 | 3.00 | ALIPAY | 已支付（回调失败待重试） | 回调失败重试用例 |

> 测试商户 `M20240101001` 的 `api_secret` 被设置为 `testsecret123` 用于本次测试，**上线前请在管理端「重置密钥」重新生成**。

---

## 6. 附加说明

- 全程应用日志无 `ERROR` 级异常；回调失败路径为预期内的 `WARN`。
- 验签过滤器、CachedBody 包装（body 可重复读取）、nonce Redis 防重放、IP 白名单均按设计工作。
- 接口文档见项目根目录 `开放API接口文档.md`，与本次实测行为一致。
