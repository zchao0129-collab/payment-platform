<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.payment.platform.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.payment.platform.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="phone" property="phone"/>
        <result column="password" property="password"/>
        <result column="salt" property="salt"/>
        <result column="role" property="role"/>
        <result column="merchant_id" property="merchantId"/>
        <result column="status" property="status"/>
        <result column="login_lock_until" property="loginLockUntil"/>
        <result column="login_fail_count" property="loginFailCount"/>
        <result column="last_login_time" property="lastLoginTime"/>
        <result column="last_login_ip" property="lastLoginIp"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, username, phone, password, salt, role, merchant_id, status,
        login_lock_until, login_fail_count, last_login_time, last_login_ip,
        created_at, updated_at
    </sql>

    <sql id="Where_Condition">
        <where>
            <if test="username != null and username != ''">
                AND username = #{username}
            </if>
            <if test="phone != null and phone != ''">
                AND phone = #{phone}
            </if>
            <if test="role != null">
                AND role = #{role}
            </if>
            <if test="merchantId != null">
                AND merchant_id = #{merchantId}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </sql>

    <select id="selectByCondition" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_user
        <include refid="Where_Condition"/>
        ORDER BY created_at DESC
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_user
        WHERE username = #{username}
    </select>

    <select id="selectByPhone" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_user
        WHERE phone = #{phone}
    </select>

    <select id="selectByMerchantId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_user
        WHERE merchant_id = #{merchantId}
    </select>

    <update id="updateStatus">
        UPDATE t_user
        SET status = #{status}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateLoginFail">
        UPDATE t_user
        SET login_fail_count = login_fail_count + 1,
            login_lock_until = #{loginLockUntil},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="resetLoginFail">
        UPDATE t_user
        SET login_fail_count = 0,
            login_lock_until = NULL,
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateLastLogin">
        UPDATE t_user
        SET last_login_time = NOW(),
            last_login_ip = #{ip},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>